function testFilters(filtered_values, result_to_be_tested) {
    if (filtered_values === result_to_be_tested) {
        console.log('Test passed');
    } else {
        console.log('Test failed');
    }
}